请审查 GitLab MR #{{.MRIID}}（{{.Title}}）的代码变更，并在完成后将**审查结论**通过飞书互动卡片通知作者及**指定群组**。

MR 信息：
- 项目: {{.ProjectName}} (ID: {{.ProjectID}})
- 标题: {{.Title}}
- 描述: {{.Description}}
- 源分支: {{.SourceBranch}} → 目标分支: {{.TargetBranch}}
{{if .AuthorEmailFromWebhook}}- Webhook 同步邮箱（API 无 author_email 时可用于飞书私信匹配）: {{.AuthorEmailFromWebhook}}
{{end}}

## 前置条件

- DMR 已启用 **`dmr-plugin-feishu-notify`**，配置好默认模板。
- 飞书卡片变量 key 与本提示中 `template_variable` 一致。

| 建议变量 key | 含义 |
|-------------|------|
| `mr_title` | MR 标题 |
| `mr_url` | `web_url`（gitlabGetMrMeta） |
| `project_path` | 项目可读标识 |
| `branch_info` | `{{.SourceBranch}} → {{.TargetBranch}}` |
| `review_summary` | 1～3 句结论 |
| `risk_level` | 如：`无阻塞` / `建议修改` / `需关注` |

## 执行步骤

1. `gitlabGetMrMeta` — 取 `web_url`、`author_email` 等。

2. `gitlabGetMrDiff` — 通读变更。

3. 使用 **`skill security-scan`** 对变更代码进行安全扫描，识别潜在的安全漏洞（如注入、XSS、硬编码密钥等）。

4. 按 {{.ReviewLanguage}} 审查：关注质量、缺陷、安全（结合 security-scan 结果）、性能。

5. `gitlabPostComment` 发布**完整**审查总结（Markdown），包含 security-scan 发现的安全问题。

6. 对具体代码问题调用 `gitlabPostDiscussion`（`file_path`, `new_line`, `body`）。

7. 调用 **`feishuNotifyByEmail`**：
   - `emails`：`gitlabGetMrMeta` 的 `author_email` 非空则优先；**否则若上文 MR 信息中 Webhook 邮箱非空**则 `["该邮箱"]`；再否则 `[]`（仅发群）。
   - `chat_id`: "oc_YOUR_GROUP_CHAT_ID"
   - `template_variable`：填齐飞书变量；`review_summary` 从步骤 4 总结中**压缩**成 1～3 句结论。
   - 无需 `template_id` 时使用插件默认。
   - 即使邮箱解析失败，只要 `chat_id` 有效仍会向群发送。

8. 调用 `tape.handoff`（`name` 建议 `gitlab:mr:{{.MRIID}}:review-done:<UTC时间戳>`，`summary` 可写 `review+feishu notify done for mr_iid={{.MRIID}}`）。

审查语言：{{.ReviewLanguage}}
