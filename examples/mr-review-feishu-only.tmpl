请审查 GitLab MR #{{.MRIID}}（{{.Title}}）的代码变更，**仅通过飞书**输出审查结果；**不要在 GitLab MR 上发评论或行内讨论**。

MR 信息：
- 项目: {{.ProjectName}} (ID: {{.ProjectID}})
- 标题: {{.Title}}
- 描述: {{.Description}}
- 源分支: {{.SourceBranch}} → 目标分支: {{.TargetBranch}}
{{if .AuthorEmailFromWebhook}}- Webhook 同步邮箱（GitLab API 常隐藏 author_email 时可用；来自 last_commit.author 或 user）: {{.AuthorEmailFromWebhook}}
{{end}}

## 硬性约束

- **禁止**调用 `gitlabPostComment`。
- **禁止**调用 `gitlabPostDiscussion`。
- 允许且需要： `gitlabGetMrMeta`、`gitlabGetMrDiff`（仅用于读 MR，不在 GitLab 写任何内容）。

## 前置条件

- DMR 已启用 **`dmr-plugin-feishu`**（飞书机器人插件）。
- 在 DMR 配置中设置飞书群 `chat_id`（或在调用工具时传入）。

## 执行步骤

1. `gitlabGetMrMeta` — 取 `web_url`、`author_email` 等。

2. `gitlabGetMrDiff` — 阅读变更并完成审查推理。

3. 整理审查结果为 Markdown 格式文本，包含：
   - MR 基本信息（标题、链接、分支）
   - 审查总结（1～3 句结论）
   - 具体问题列表（按严重程度分级）
   - 修复建议
   - 审查完成时间（使用当前实际时间，格式如：2026-03-30 15:30 UTC）
   - **注意：不要在消息中包含敏感信息（密码、token、密钥、凭证等），即使在代码审查中发现了此类问题，也只描述问题类型和位置，不要引用具体的敏感值**

4. 调用 **`feishuSendText`** 发送审查结果：
   - `text`：上一步整理的完整 Markdown 文本
   - `markdown`：`true`（启用飞书富文本渲染）
   - `chat_id`：飞书群 ID（如 `oc_xxx`；若 DMR 配置中已设置可省略）

   示例文本格式：
   ```
   ## GitLab MR 审查结果

   **项目**: {{.ProjectName}}
   **MR**: #{{.MRIID}} {{.Title}}
   **链接**: [查看 MR](<web_url>)
   **分支**: {{.SourceBranch}} → {{.TargetBranch}}

   ### 审查总结
   <1～3 句结论>

   ### 🔴 Critical 问题
   1. <问题描述>
      - 文件: <file:line>
      - 风险: <风险说明>
      - 建议: <修复建议>

   ### 🟠 High 问题
   ...
   ```

5. 调用 `tape.handoff`（`name` 建议 `gitlab:mr:{{.MRIID}}:review-done:<UTC时间戳>`，`summary` 可写 `feishu-only review done`）。
   - UTC 时间戳格式：`20060102-150405`（年月日-时分秒）
   - **使用当前实际时间**（2026 年 3 月），不要使用过去的日期

审查语言：{{.ReviewLanguage}}
