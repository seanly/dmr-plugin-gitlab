请审查 GitLab MR #{{.MRIID}}（{{.Title}}）的代码变更，并在完成后将**审查结论**通过飞书互动卡片通知作者（及可选群组）。

MR 信息：
- 项目: {{.ProjectName}} (ID: {{.ProjectID}})
- 标题: {{.Title}}
- 描述: {{.Description}}
- 源分支: {{.SourceBranch}} → 目标分支: {{.TargetBranch}}
{{if .AuthorEmailFromWebhook}}- Webhook 同步邮箱（API 无 author_email 时可用于飞书私信匹配）: {{.AuthorEmailFromWebhook}}
{{end}}

## 前置条件（由运维/你在 DMR 配好即可）

- DMR 主配置中已启用外部插件 **`dmr-plugin-feishu-notify`**，且 `default_template_id`、`default_template_version_name`、（建议）`default_chat_id` 已配置。
- 飞书开放平台中已创建**互动卡片模板**，模板变量名需与下文 `template_variable` 的 **key 完全一致**（若你的卡片用中文变量名，请在下发时将 key 改成与后台一致，并保持本提示词里描述的含义）。
- 建议卡片变量与下表对应（可按需增删，但工具调用里的 key 必须与你的飞书模板一致）：

| 建议变量 key | 含义 |
|-------------|------|
| `mr_title` | MR 标题 |
| `mr_url` | MR 页面链接（来自 gitlabGetMrMeta 的 `web_url`） |
| `project_path` | 仓库路径说明，可用 `{{.ProjectName}}` 或 meta 里能拿到的路径 |
| `branch_info` | 如 `{{.SourceBranch}} -> {{.TargetBranch}}` |
| `review_summary` | 给通知用的**简短结论**（一两句话，不堆砌全文） |
| `risk_level` | 可选：`无阻塞` / `建议修改` / `需关注` 等 |

## 执行步骤

1. **元数据**：调用 `gitlabGetMrMeta`（`project_id={{.ProjectID}}`, `mr_iid={{.MRIID}}`），记录 `web_url`、作者相关字段；若存在 `author_email` 则用于飞书私信。

2. **差异**：调用 `gitlabGetMrDiff`（`project_id={{.ProjectID}}`, `mr_iid={{.MRIID}}`），通读变更。

3. **审查**：按 {{.ReviewLanguage}} 关注质量、缺陷、安全、性能等；对需改点准备行内意见文案。

4. **GitLab 总结**：调用 `gitlabPostComment` 发布**完整**审查总结（Markdown，`project_id={{.ProjectID}}`, `mr_iid={{.MRIID}}`）。

5. **行内**：对具体代码问题调用 `gitlabPostDiscussion`（`project_id={{.ProjectID}}`, `mr_iid={{.MRIID}}`, `file_path`, `new_line`, `body`）。

6. **飞书通知**：在 GitLab 评论已发出后，调用 **`feishuNotifyByEmail`**：
   - `emails`：`gitlabGetMrMeta` 的 `author_email` 非空则优先；**否则若上文 MR 信息中 Webhook 邮箱非空**则 `["该邮箱"]`；再否则 **`[]`**（仅 `default_chat_id` 群发）。
   - `template_variable`：**对象**，键名必须与你的飞书卡片变量一致，值示例：
     - `mr_title`: 「{{.Title}}」或 meta 中标题
     - `mr_url`: meta 的 `web_url`
     - `project_path`: 可读项目标识
     - `branch_info`: `{{.SourceBranch}} → {{.TargetBranch}}`
     - `review_summary`: 从步骤 4 的总结中**压缩**成 1～3 句结论（问题数量、是否阻塞合并、优先修什么）
     - `risk_level`: 你根据审查结果选的短标签
   - 不传 `template_id` / `template_version_name` 时沿用插件默认模板。
   - 若作者邮箱解析失败或未配置作者邮箱，只要 `default_chat_id` 有效，仍应向群发送，避免“零通知”。

7. **handoff**：最后调用 `tape.handoff`，避免下次同 MR 上下文爆炸：
   - `name` 建议：`gitlab:mr:{{.MRIID}}:review-done:<UTC时间戳>`（时间戳格式建议 `20060102-150405`）
   - `summary` 可选：`review+feishu notify done for mr_iid={{.MRIID}}`

审查语言：{{.ReviewLanguage}}
