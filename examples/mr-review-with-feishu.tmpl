请审查 GitLab MR #{{.MRIID}}（{{.Title}}）的代码变更，并在完成后将**审查结论**通过飞书消息通知群组。

MR 信息：
- 项目: {{.ProjectName}} (ID: {{.ProjectID}})
- 标题: {{.Title}}
- 描述: {{.Description}}
- 源分支: {{.SourceBranch}} → 目标分支: {{.TargetBranch}}

## 前置条件

- DMR 已启用 **`dmr-plugin-feishu`**（飞书机器人插件）。
- 在 DMR 配置中设置飞书群 `chat_id`（或在调用工具时传入）。

## 执行步骤

1. **元数据**：调用 `gitlabGetMrMeta`，记录 `web_url`、作者相关字段；若存在 `author_email` 则用于飞书私信。

2. **差异**：调用 `gitlabGetMrDiff`，通读变更。

3. **审查**：按 {{.ReviewLanguage}} 关注质量、缺陷、安全、性能等；对需改点准备行内意见文案。

4. **GitLab 总结**：调用 `gitlabPostComment` 发布**完整**审查总结（Markdown）。

5. **行内**：对具体代码问题调用 `gitlabPostDiscussion`（`file_path`, `new_line`, `body`）。

6. **飞书通知**：在 GitLab 评论已发出后，调用 **`feishuSendText`** 发送审查结论到群：
   - `text`：整理为 Markdown 格式的简短通知，包含：
     - MR 基本信息（项目、标题、链接、分支）
     - 审查总结（1～3 句结论，问题数量、风险等级）
     - 提示”详细审查意见已发布到 GitLab MR”
     - **注意：不要在飞书消息中包含敏感信息（密码、token、密钥、凭证等），只描述问题类型和严重程度，具体内容请用户到 GitLab MR 查看**
   - `markdown`：`true`（启用飞书富文本渲染）
   - `chat_id`：飞书群 ID（如 `oc_xxx`；若 DMR 配置中已设置可省略）

   示例文本格式：
   ```
   ## GitLab MR 审查完成

   **项目**: {{.ProjectName}}
   **MR**: #{{.MRIID}} {{.Title}}
   **链接**: [查看 MR](<web_url>)
   **分支**: {{.SourceBranch}} → {{.TargetBranch}}

   **审查结论**: <1～3 句总结>
   **风险等级**: <无阻塞/建议修改/需关注>

   详细审查意见和行内评论已发布到 GitLab MR，请查看。
   ```

7. **handoff**：最后调用 `tape.handoff`，避免下次同 MR 上下文爆炸：
   - `name` 建议：`gitlab:mr:{{.MRIID}}:review-done:<UTC时间戳>`（时间戳格式建议 `20060102-150405`）
   - `summary` 可选：`review+feishu notify done for mr_iid={{.MRIID}}`
   - **使用当前实际时间**（2026 年 3 月），不要使用过去的日期

审查语言：{{.ReviewLanguage}}
